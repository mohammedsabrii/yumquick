class CategoryItemModel {
  final String image, title, price, subTitle;

  CategoryItemModel({
    required this.image,
    required this.title,
    required this.price,
    required this.subTitle,
  });
}
